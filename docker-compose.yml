version: '3'
services:
  github-bot:
    container_name: octogpt
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
    restart: always
    command: ['pnpm', 'start']
